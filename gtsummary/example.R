# open mtcars dataset
mtcars
View(mtcars)
str(mtcars)
# database structure of mtcars
str(mtcars)
# database summary of mtcars
summary(mtcars)
# database head of mtcars
head(mtcars)
# database tail of mtcars
tail(mtcars)
# database random of mtcars
random(mtcars)
